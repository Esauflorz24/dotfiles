# dotfiles
my dotfiles :)

# `Zsh syntax highlighting `

Install package with AUR `yay -S zsh-syntax-highlighting-git` and then copy the following line to the `.zshrc`
```
source /usr/share/zsh/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
```
